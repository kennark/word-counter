# Word Counter

Backend API for a word counter. Exposes 2 endpoints at localhost:8080  
POST `/upload` - uploads the text files  
GET `/upload/:id?mincount=1` - shows the word counts to the user, `minCount` filters out words with count lower than the given  

Simple project to demonstrate use of Spring Boot.
